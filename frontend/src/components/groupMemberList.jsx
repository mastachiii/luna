export default function GroupMemberList({ members, ownerId }) {
    console.log({ members, ownerId });

    return <div className="w-[17%] h-full overflow-y-scroll bg-zinc-200 scroll-p-100">HELLO REACT</div>;
}
